<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Prototype only: cleartext ws:// is allowed so a phone on the same trusted LAN can reach the local bridge. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
